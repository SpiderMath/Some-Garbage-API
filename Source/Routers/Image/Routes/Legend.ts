import RouteExport from "../../../Constants/RouteExport";

const LegendRoute: RouteExport = {
	name: "legend",
	description: "Makes the person a Legend!",
	async run(req, res) {
		try {}
		catch(err) {
			res.send("");
		}
	},
};

export default LegendRoute;